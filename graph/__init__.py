"""Stage 9 LangGraph orchestration layer.

This package coordinates existing services. It intentionally does not replace the
planner, executor, memory service, RAG pipeline, tool registry, or approval service.
"""
